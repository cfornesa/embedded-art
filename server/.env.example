# Server
PORT=3000
BASE_PATH=/threejs

# MySQL
DB_HOST=localhost
DB_NAME=embedded_art
DB_USER=your_user
DB_PASS=your_password

# Optional: CORS (comma-separated list)
ALLOWED_ORIGINS=*

# Optional: debug endpoints
ENABLE_DEBUG_ENDPOINTS=0
ENABLE_DEBUG_LOGGING=0

# Optional: SMTP for email notifications
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
